---
all:
  children:
    kite_windows:
      hosts:
        # Use Active Directory FQDNs; Kerberos should not connect by IP.
        pc-001.corp.example.com:
        # pc-002.corp.example.com:

    # Start every release against this group before expanding the rollout.
    kite_windows_pilot:
      hosts:
        pc-001.corp.example.com:
