{
  "options": {
    "disable_events": "true"
  },
  "schedule": {
    "processes_snapshot": {
      "query": "SELECT pid, name FROM processes LIMIT 20;",
      "interval": 60,
      "snapshot": true
    }
  }
}
